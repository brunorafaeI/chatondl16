{% extends 'base.html.twig' %}

{% block body %}
    <h1>Chatons list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Qualite</th>
                <th>Marquecroquettes</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for chaton in chatons %}
            <tr>
                <td><a href="{{ path('chaton_show', { 'id': chaton.id }) }}">{{ chaton.id }}</a></td>
                <td>{{ chaton.nom }}</td>
                <td>{{ chaton.qualite }}</td>
                <td>{{ chaton.marqueCroquettes }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('chaton_show', { 'id': chaton.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('chaton_edit', { 'id': chaton.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('chaton_new') }}">Create a new chaton</a>
        </li>
    </ul>
{% endblock %}
